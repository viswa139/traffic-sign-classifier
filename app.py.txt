from flask import Flask, jsonify

app = Flask(__name__)

@app.route('/')
def home():
    return "Traffic Sign Classifier API is running"

@app.route('/predict', methods=['POST'])
def predict():
    # Simulated result
    return jsonify({"class": "Stop", "confidence": 0.95})

if __name__ == '__main__':
    app.run(debug=True)
